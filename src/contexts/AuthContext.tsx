
// We need to add 'faculty' to the UserRole type but this file is read-only.
// Instead, we'll need to make sure our components handle this role properly.
// The database migration added 'faculty' to the user_role enum, so it will work at runtime.
