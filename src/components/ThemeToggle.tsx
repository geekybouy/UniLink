
import React from 'react';

// This component is now a placeholder that renders nothing
// since we're removing dark mode functionality
export function ThemeToggle({ className }: { className?: string }) {
  return null;
}

export default ThemeToggle;
