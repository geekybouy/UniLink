import React from 'react';
// Remove: import { SocialLink } from "@/types/profile";

const SocialLinksStep = (props: any) => {
  return (
    <div>
      <h3>Social Links (not implemented in DB schema)</h3>
      <p className="text-muted-foreground">Social links collection UI here (not stored in UserProfile)</p>
    </div>
  );
};
export default SocialLinksStep;
